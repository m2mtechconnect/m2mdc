# PR-0.1 Checkpoint B7.4B - Controlled-Pilot Route Declaration

**Status:** B7.4B READY - ZERO-FUNCTION PILOT PROVEN (public surface). Approved-user pilot DEFERRED.
**Anchor:** git HEAD `a1da5a1f01e673209579b3a95a574d50d834f0dc`
**Mode:** no function was hardened, allowlisted, or deployed; no remote infrastructure was contacted; the real function allowlist (`production_functions`) remains empty.

## What changed vs B7.4A

| File | Change |
|---|---|
| `docs/remediation/evidence/pr-0.1/route-allowlist.json` | `/reset-password` removed; `/forgot-password`, `/sign-out`, `/onboarding` added to `production_routes`. New fields: `production_blocked_routes[]`, `development_only_routes[]`, `redirect_only_routes[]`. `production_functions` still empty. |
| `scripts/verify-production-perimeter.mjs` | Added rule #8 (NEG-A: every `<Route path="...">` must be classified or DEV-gated) and rule #9 (NEG-D: no intersection between `production_functions` and `disabled_functions`). |
| `scripts/__tests__/perimeter-negative.mjs` | Added NEG-A, NEG-B, NEG-C, NEG-D, NEG-F, NEG-I as isolated scratch fixtures. Added CLEAN control against the real repo. All 10 fixtures behave as expected. |
| Evidence bundle | new files: `pilot-scope-decision.json`, `password-recovery-reconciliation.md`, `b7.4b-summary.md`; regenerated `pilot-route-matrix.tsv`, `function-callsite-reconciliation.tsv`, `pilot-function-candidates.json`, `route-classification.tsv`, `negative-tests-consolidated.json`. |

## 1. Evidence anchor

- git HEAD: `a1da5a1f01e673209579b3a95a574d50d834f0dc`
- Reconciliation baseline: **63 distinct functions across 92 call sites**.
- Why B6 said 49/71: the B6 pass only counted `supabase.functions.invoke(` sites and pre-dated copilot-health, several zapier-*, knowledge-index, reco-selftest, workflow-run, funding-scraper and additional url-*/rag-*/ops-overview sites, and the `invokeEdgeFunction` wrapper in `src/hooks/useEdgeFunction.ts`. Re-counting with `rg -n 'functions\.invoke|invokeEdgeFunction'` produces 63/92.

## 2. Consolidated B7.3 result

All eight required negative axes now execute as isolated scratch fixtures. Positive control and clean control also pass. No case is SKIP or NOT-IMPLEMENTED.

| Case | Axis | Expected | Observed | Result |
|------|------|----------|----------|--------|
| NEG-A | unclassified route              | exit 1 | exit 1 | PASS |
| NEG-B | unclassified function           | exit 1 | exit 1 | PASS |
| NEG-C | wildcard function deployment    | exit 1 | exit 1 | PASS |
| NEG-D | disabled fn re-allowlist        | exit 1 | exit 1 | PASS |
| NEG-E | allowlisted fn missing authz    | exit 1 | exit 1 | PASS |
| NEG-F | forbidden browser env identifier| exit 1 | exit 1 | PASS |
| NEG-G | production test/debug route     | exit 1 | exit 1 | PASS |
| NEG-I | verify_jwt=false unauthorized   | exit 1 | exit 1 | PASS |
| NEG-H | DEV-gated route (positive)      | exit 0 | exit 0 | PASS |
| CLEAN | real repo tree at HEAD          | exit 0 | exit 0 | PASS |

Command: `node scripts/__tests__/perimeter-negative.mjs`. Full observed-vs-expected identities recorded in `negative-tests-consolidated.json`. NEG-A and NEG-D close the two B7.3 gaps identified earlier.

## 3. Password-recovery disposition

- Entry: `/forgot-password` - FUNCTIONAL. No edge function.
- Completion: `/reset-password` - BROKEN. Route not declared; email `redirectTo` lands on wildcard (`Navigate to "/"` unauth, `NotFound` authed, `PendingApproval` not-approved). No page exists.
- Action taken: `/reset-password` removed from the allowlist; no route added. Recovery-completion is documented BROKEN, not silently omitted.

Full evidence: `password-recovery-reconciliation.md`.

## 4. Total routes by classification

| Classification         | Count | Notes |
|------------------------|-------|-------|
| public-pilot           | 5     | `/`, `/login`, `/forgot-password`, `/sign-out`, `/onboarding` |
| approved-user-pilot    | 0     | DEFERRED - Layout mounts excluded capabilities |
| production-blocked     | ~48   | includes wildcard `*` and dual-mounted redirect targets classified as blocked in their non-redirect branch |
| development-only       | 1     | `/dev-overlays` (DEV-gated on all mount sites) |
| redirect-only          | 7     | `/integrations`, `/agents`, `/subsystem-agents`, `/digital-twins`, `/digital-twins/:slug`, `/app/agents/:agentId/operations`, `/twins/:instanceId/manage` |

Complete row-per-route breakdown: `route-classification.tsv`.

## 5. Exact approved pilot routes

`/`, `/login`, `/forgot-password`, `/sign-out`, `/onboarding`. Minimum role: anonymous (no `security_admin`, no elevated role added to make the pilot work).

## 6. Pilot workflows exercised

- Public landing at `/`: render truthfully, click through to sign-in.
- Sign-in at `/login`: Supabase Auth SDK, no edge function.
- Password-recovery entry at `/forgot-password`: Supabase Auth SDK, no edge function. Completion is broken.
- Onboarding at `/onboarding`: 4-step lead capture, direct `public.profiles` insert (SDK), no edge function.
- Logout at `/sign-out`: `supabase.auth.signOut`, no edge function.

All five pilot workflows operate without any edge function invocation.

## 7. Reconciled function and call-site totals

- Distinct functions invoked from `src/`: **63**
- Distinct call sites: **92**
- Documentation-only strings (not real invocations): **1** (`copilot-router` in `src/components/copilot/README.md`)
- Production-unreachable (behind non-pilot routes): **62**
- Typed-accessible-unavailable on pilot routes: **0**
- Pilot-required candidates: **0**

Row-per-site table: `function-callsite-reconciliation.tsv`.

## 8. Pilot-required function candidates

None. See `pilot-function-candidates.json`.

## 9. Broken, unavailable or deferred capabilities

- BROKEN: password-recovery completion (`/reset-password` not implemented).
- DEFERRED: primary AURA overview, read-only asset/metric inspection, provenance-inspection surface, read-only export, availability-state inspection - all four approved-user pilot capabilities require B7.5 to either de-couple Layout from excluded capabilities or fully assess and allowlist the affected functions.
- TYPED-UNAVAILABLE (in existing UI, non-pilot): `health`, `copilot-health`, `ops-overview`, and various badges already render an accessible failure state on non-2xx; they are safe under default-deny but are not on pilot routes.

## 10. Recommended next authorization

Two orthogonal follow-ups, in either order:

1. **B7.5 Layout de-coupling.** Extract the excluded capability mounts (Copilot bubble/panel, health badges, GlobalSearchBar suggestions, `useTokenRefresh` zapier hooks) behind a `pilotLayout` flag and route configuration, so an approved-user pilot page can be selected without transitively invoking blocked functions. This unlocks the deferred pilot capabilities without any function allowlisting.
2. **Password-recovery completion.** Decide whether to (a) add a real `/reset-password` page and route (then re-open B7.4B for that route), or (b) update the `redirectTo` in `ForgotPassword.tsx` to a route that clearly communicates recovery-completion is not yet available.

No B7.5/B7.6/Checkpoint C work is started. No function is deployed. No remote infrastructure is touched.
