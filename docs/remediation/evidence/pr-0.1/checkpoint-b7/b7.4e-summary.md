# PR-0.1 Checkpoint B7.4E — Local Pilot Read Path

Anchor: `b1f26438195f141fbc5130f18a3bd6e0133e5ac5`
Inherits: B7.4C, B7.4D verdicts verbatim; B7.4D remains **BLOCKED — NO TRUTHFUL READ-ONLY DATA PATH** as the pre-expansion decision record. This checkpoint does not rewrite B7.4D.

## Preserved artifacts (checksums at inherit time)

| File | SHA-256 |
|---|---|
| checkpoint-b7/b7.4d-summary.md | 5f616cb0dfa547bc33534b5625b5d7214869c2e2fb0830be72de94d67ac2a7f7 |
| checkpoint-b7/b7.4c-summary.md | 7346e372b8184f29db45bde8b88b0227c0113e8c0e3ae12c0bebd46932523d9f |
| checkpoint-b7/b7.4b-summary.md | 25225393f086e5981813964894c79e163e3e2981954e2395ef94c0fbc6780a6b |

No remote Supabase or Lovable Cloud infrastructure was contacted. No migrations, tables, views, policies, grants or database functions were added or modified. No edge functions were changed or deployed. No production data was seeded. The production function allowlist remains empty (0 allowlisted, 155 inventoried).

## Selected read model

`public.data_centre_twins` (overview) and `public.twin_kpi_snapshots` (inspection), both gated by pre-existing owner-scoped RLS SELECT policies. Full disposition and contract:

- `checkpoint-b7/pilot-read-model-disposition.tsv`
- `checkpoint-b7/pilot-data-contract.md`

## Implemented surface

- `src/pilot/pilotReadAdapter.ts` — SELECT-only, explicit column projection, bounded LIMIT, sanitized errors, no fallback rows on failure. Freshness classifier surfaces `fresh` / `stale` / `unvalidated` — never fabricated as fresh.
- `src/pilot/PilotLayout.tsx` — pilot-only shell rendered OUTSIDE `src/components/Layout`. Imports: react, react-router-dom, `@/integrations/supabase/client` only. Does not mount `HealthBadges`, `useTokenRefresh`, `GlobalSearchBar`, `CoPilotPanel`, `CoPilotBubble`, `CoPilotProvider*`, or any consumer of blocked edge functions.
- `src/pilot/PilotOverview.tsx`, `src/pilot/PilotAssetInspection.tsx` — the two authorized routes.
- `src/pilot/PilotShell.tsx` — nested `<Routes>` mounting the layout and children under `/pilot/*`.
- `src/App.tsx` — approved-user branch now returns `<Routes>` with `/pilot/*` → `PilotShell` and `*` → the legacy `<Layout>` tree (unchanged behaviour for existing production-blocked routes).

Authorization gate: `AuthenticatedApp` in `src/App.tsx` continues to require an authenticated user AND `profiles.is_approved = true` before this branch mounts. Unapproved users see `PendingApproval`; unauthenticated users see the public shell. Client routing is a UI boundary; the authoritative boundary is the existing RLS policy on the selected tables.

## Runtime dependency reconciliation

See `checkpoint-b7/pilot-data-dependency-matrix.b7.4e.tsv` and `checkpoint-b7/approved-user-pilot-routes.b7.4e.tsv`. Aggregate:

- Edge-function invocations: **0**
- Application-data writes: **0**
- RPC calls: **0**
- Storage mutations: **0**
- Realtime subscriptions: **0**
- External requests: **0** (beyond Supabase Auth `getUser`/`signOut` and two SELECTs)

## Local verification

See `checkpoint-b7/b7.4e-test-results.json`.

- `bunx tsgo --noEmit` — PASS
- `bunx eslint src/pilot/ src/App.tsx --max-warnings 0` — PASS
- `node scripts/verify-production-perimeter.mjs` — PASS (route `/pilot/*` classified under `production_routes`; no new production function allowlisted)

## Deferred / not attempted under this checkpoint

- Playwright pilot-route suite and screen-reader keyboard walkthroughs — deferred to B7.5.
- Bundle canary scanner — no standalone script exists in `scripts/`; verification here is static via the perimeter enforcer.
- Effective remote RLS enforcement, remote authorization verification, credential rotation, remote function-undeployment verification, migration-chain replay, and confirmation that the selected read model contains validated production records — remain **external blockers** (see `docs/remediation/external-blockers.md`). A local pass here does **not** establish any of those.

## Verdict

**B7.4E READY FOR B7.5 — LOCAL PILOT READ PATH IMPLEMENTED**

Hard-stop. B7.5, B7.6 and Checkpoint C are not started.