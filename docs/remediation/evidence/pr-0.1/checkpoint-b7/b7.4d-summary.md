# PR-0.1 Checkpoint B7.4D - Minimal Approved-User Pilot Shell Implementation

**Verdict:** B7.4D BLOCKED - NO TRUTHFUL READ-ONLY DATA PATH
**Anchor:** git HEAD `bb4b4d8cba918d551fb76e03460dbef2db2083b1` (working tree carries only the B7.4D evidence additions listed below; no application source, migration, allowlist, edge function or remote infrastructure was modified).
**Prior anchor accepted:** `15bc43860593053c739230df0f2a3b9891ab5b55` (B7.4C).

## 1. Evidence baseline preserved

B7.4C is preserved verbatim as the truthful record of the application before decoupling. The four surviving public-shell routes are `/`, `/login`, `/forgot-password`, `/sign-out`. `/onboarding` remains `production-blocked` (write-enabled at `src/pages/Onboarding.tsx:108`). Password recovery remains **not operable** (`/forgot-password` is entry-only; `/reset-password` is absent). The production function allowlist remains empty (`route-allowlist.json` `functions: []`, 155 functions inventoried, 0 allowlisted).

### B7.4C artifact checksums at B7.4D open

```
176e5cf9cc45d565e73f13e541256b6765c55155e4f2794f0313a98a5ac99451  approved-user-pilot-routes.tsv
210bdbd013ab83c656b3a75f948beaa00c0b5b410ba13aba53d3efdb91b0ef48  pilot-data-dependency-matrix.tsv
c329cf4eb554638c6409a387f921782605aed067ba732380649dd69f552b7da0  route-classification.tsv
3787cac98550389ce64fe86fa502158a823489d23448128cc3dfa55bd3f81a11  pilot-route-matrix.tsv
16208db97350d3140a440ee11845419744783380f87d977247fe084e1fdef8db  function-callsite-reconciliation.tsv
913a96da9abf5421ff46f4b41a98bdeeec307a5e71624acb34216b7ddd9bce00  pilot-function-candidates.json
e3f6b17ca12255ed077d80e23895bcfaa6d3bfaa25718898415b5674a551c68a  pilot-scope-decision.json
6f85f8b8990c1ebccea234d4f1e65d2a5859944cf09484e121c626e4cd43a530  negative-tests-consolidated.json
f0189c4f54950658180182aeb8dff734a5438d3c316ea03b266f02bee7eed3dd  password-recovery-reconciliation.md
7346e372b8184f29db45bde8b88b0227c0113e8c0e3ae12c0bebd46932523d9f  b7.4c-summary.md
25225393f086e5981813964894c79e163e3e2981954e2395ef94c0fbc6780a6b  b7.4b-summary.md
```

### Negative-test terminology reconciled

The `negative-tests-consolidated.json` bundle contains ten cases, which are correctly labelled as follows (not "ten negative fixtures"):

- **Eight required negative cases**: NEG-A, NEG-B, NEG-C, NEG-D, NEG-E, NEG-F, NEG-G, NEG-H - each fixture must be detected by the perimeter enforcer.
- **One positive authorization-guard control**: NEG-I - allowlisted-with-guard fixture; enforcer must accept.
- **One current-tree clean control**: CLEAN - real repository at HEAD must pass all rules.

No new negative case was introduced at B7.4D. Recording the correct classification is the only change.

## 2. Smallest existing approved-user route set - search result

Requirement: one existing primary AURA overview route + one existing representative read-only asset or metric inspection route, each rendering validated operational information with visible provenance, freshness/availability, and truthful unavailable behaviour, using only existing components and no new data source.

Every existing authed candidate was assessed (see `approved-user-pilot-routes.tsv` unchanged from B7.4C):

| Candidate | Edge-function invocation on initial render | Truthful read-only data path via existing components? |
|---|---|---|
| `/`, `/dashboard` (Dashboard)                                    | `ai-systems-unified` | **no** - fabricated fallback on non-2xx; contains `systems-delete` action |
| `/analytics`, `/operations`, `/intelligence` (IntelligenceDashboard) | `ops-overview`       | **no** - blocked function on initial render; page is defined by that dataset |
| `/agent/:id`, `/agent-chat`, `/agents/:id/chat`                  | ai-gateway + copilot-* | **no** - excluded AI capability |
| `/builder`, `/deploy`, `/deployments`, `/marketplace/integrations`, `/compliance`, etc. | blocked functions   | **no** |
| `/account/profile`                                               | none (direct `profiles` row read via Supabase Auth SDK path) | **partial** - a read exists, but this is not a primary AURA overview and it is not a representative asset/metric inspection surface; selecting it would misrepresent the pilot |
| `/account/settings`                                              | none                  | **no** - writes on save |
| `/help`                                                          | none on load          | **no** - static content; also `supabase.from('contact_expert_logs').insert(...)` at `src/pages/Help.tsx:123` on user action; not an overview or inspection surface |
| `/sign-out`                                                      | none                  | logout only |

No existing route pair satisfies "primary AURA overview" + "representative read-only asset or metric inspection" using only existing components and existing authorized read paths. The only zero-edge-function existing surfaces (`/account/profile`, `/help` at load) are neither overview nor inspection surfaces and cannot be truthfully re-cast as such without introducing new product content or a new data source. Doing so would violate the hard boundary "You may not: introduce a new data source or product workflow" and "You may not: fabricate, simulate or hard-code production metrics".

The authorized escape hatch in step 2 therefore applies:

> "If no existing truthful read-only data path exists, stop with: **B7.4D BLOCKED - NO TRUTHFUL READ-ONLY DATA PATH**"

## 3. Pilot-safe shell - not introduced at B7.4D

A minimal `PilotLayout` was designed on paper (product identity + approved pilot navigation + current-user identity + logout, with `HealthBadges`, `useTokenRefresh`, `GlobalSearchBar`, `CoPilotPanel`, `CoPilotBubble` and all blocked-function consumers absent from the import graph, not merely hidden). It is not committed at this checkpoint because with no truthful overview/inspection page to mount inside it, committing the shell alone would neither prove operability nor close the identified gap - and would introduce dead product surface into the tree. The shell is deferred to a future checkpoint that either (a) delivers an approved overview/inspection route replacement or (b) restores a specific edge function under an explicit function-disposition decision (path deferred to B7.5 planning).

## 4. Route boundary and authorization

No route boundary changes were made at B7.4D. The existing enforcement remains:

- Unauthenticated users are held at `/`, `/login`, `/forgot-password`, `/sign-out` (public shell).
- Authenticated-but-unapproved users are pinned to `PendingApproval` for `*` (`src/App.tsx:195`).
- Approved users enter `<Layout>` for every authed route; every such route therefore initializes excluded capabilities as documented in B7.4C.
- `/reset-password` is not routed and no completion page exists. Recovery remains not operable.

Client-side routing is not the database security boundary. Effective remote RLS on `profiles`, `user_roles`, `onboarding_submissions`, `contact_expert_logs` remains **unverified from the sandbox** and is retained as an external blocker.

## 5. Dependency reconciliation - unchanged from B7.4C

`pilot-data-dependency-matrix.tsv` at anchor is authoritative. Summary: every candidate authed route either invokes at least one blocked edge function on initial render or on user action, or is not an overview/inspection surface, or performs a write. No zero-edge-function, zero-write, overview+inspection pair exists in the current tree using only existing components.

No essential edge function is put forward for allowlisting at B7.4D. This checkpoint does not return "B7.4D READY FOR FUNCTION DISPOSITION" because the blocker is the absence of a truthful data path, not the presence of an essential-but-blocked function; nominating a function without an approved overview/inspection page would prejudge B7.5.

## 6. Production-build exercise

Not run. No pilot surface exists to exercise. Public-shell behaviour (anonymous denial of authed routes, `/`, `/login`, `/forgot-password`, `/sign-out` operable, no edge-function traffic) is unchanged from B7.4B/B7.4C and is not re-collected here.

## 7. Evidence delivered at B7.4D

| File | Status at B7.4D |
|---|---|
| `approved-user-pilot-routes.tsv`       | UNCHANGED from B7.4C (row-per-candidate assessment still authoritative) |
| `pilot-data-dependency-matrix.tsv`     | UNCHANGED from B7.4C |
| `route-classification.tsv`             | UNCHANGED from B7.4C (`/onboarding` remains production-blocked) |
| `pilot-route-matrix.tsv`               | UNCHANGED from B7.4C |
| `function-callsite-reconciliation.tsv` | UNCHANGED (63 functions across 92 sites) |
| `pilot-function-candidates.json`       | UNCHANGED (`pilot_required_candidate_count = 0`) |
| `pilot-scope-decision.json`            | UNCHANGED (approved-user pilot BLOCKED) |
| `negative-tests-consolidated.json`     | UNCHANGED payload; terminology reconciled in this summary (8 required negatives + 1 positive authz control NEG-I + 1 CLEAN control) |
| `password-recovery-reconciliation.md`  | UNCHANGED (recovery not operable) |
| `b7.4c-summary.md`                     | PRESERVED verbatim as the truthful pre-decoupling record |
| `b7.4b-summary.md`                     | PRESERVED (public-route findings + superseding banner from B7.4C) |
| `b7.4d-summary.md`                     | NEW (this document) |

No application source, migration, edge function, allowlist or remote infrastructure was modified. The perimeter enforcer continues to pass on the real repository tree (155 functions inventoried, 0 allowlisted).

## 8. External blockers retained

- Credential rotation (`VITE_LOVABLE_API_KEY`, Omniverse host) - out-of-band.
- Effective remote authorization and RLS verification on `profiles`, `user_roles`, `onboarding_submissions`, `contact_expert_logs` - requires an approved Supabase-compatible environment.
- Remote function-undeployment verification for the 155 inventoried functions - not verifiable from the sandbox.
- Accidental migration change-management reconciliation - unchanged.
- Full migration-chain replay in an approved Supabase-compatible environment.

## 9. Verdict

**B7.4D BLOCKED - NO TRUTHFUL READ-ONLY DATA PATH**

No existing route pair in the current tree can serve as both a primary AURA overview and a representative read-only asset/metric inspection surface without either (a) invoking a blocked edge function on initial render, (b) rendering fabricated fallback data, or (c) introducing a new data source or product workflow - each of which is prohibited by the B7.4D hard boundary. B7.5, B7.6 and Checkpoint C remain not started per the hard-stop.