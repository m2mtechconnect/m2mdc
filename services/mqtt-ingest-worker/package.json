{
  "name": "aura-mqtt-ingest-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Persistent MQTT ingestion worker for AURA connections. Runs on Brev/AWS or any container host; Supabase Edge Functions cannot hold a long-lived broker subscription.",
  "scripts": {
    "start": "tsx src/index.ts",
    "once": "tsx src/index.ts --once"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0",
    "mqtt": "^5.15.2",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}