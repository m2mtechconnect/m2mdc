# Export download inspection

Status: **BLOCKED_UNVERIFIED**. No CSV or JSON artifact was downloaded from the
published host in this phase, so MIME type, filename, parseability, dataset and
record lineage, checksums and screen/export scope agreement remain uninspected.
Export captions and per-page export identity are unit-asserted only
(`pageIdentity.test.ts`, `exportProvenance.ts`).
