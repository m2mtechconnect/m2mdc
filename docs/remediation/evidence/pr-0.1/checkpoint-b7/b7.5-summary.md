# PR-0.1 Checkpoint B7.5 — Migration-Order Static Inventory

**Baseline anchor:** `47b2ef8ca8bbf8d407cfa46086c78b5f23a70a6e`
**Working tree:** clean at start of checkpoint; only B7.5 evidence written.
**Remote infrastructure contacted:** NO.
**Migrations executed:** NO.
**Verdict:** **STATIC ORDER VERIFIED — COMPLETE-CHAIN EXECUTION PENDING.**

## What was produced

All artifacts under `docs/remediation/evidence/pr-0.1/checkpoint-b7/b7.5/`:

- `migration-order-inventory.tsv` — 18 rows, one per repository migration, in strict lexicographic (Supabase CLI) order. Columns: order, filename, timestamp, SHA-256, git origin, purpose, external prereqs, risks.
- `migration-object-lifecycle.tsv` — per-migration list of objects created / altered / dropped and whether data was mutated.
- `migration-dependency-graph.json` — machine-readable producer → consumer edges (baseline dump feeds every later migration; P0 introducer feeds B1 corrective).
- `migration-risk-register.tsv` — risks classified P0 / P1 / P3 with mitigation reference.
- `migration-replay-specification.md` — disposable-environment replay contract (extensions, roles, preflight, post-replay authorization assertions, teardown). Explicitly forbids running against production or Lovable Cloud.
- `b1-migration-static-reconciliation.md` — narrative reconciliation of the B1 corrective migration.
- `b1-reconciliation-output.json` — raw output of `scripts/verify-b1-static-reconciliation.mjs` (26/26 checks pass).
- `inventory-validator.json` — duplicate/malformed/empty scan results.
- `b7.5-test-results.json` — machine-readable roll-up.
- `SHA256SUMS` — integrity for every artifact above.

## Chain shape

- 18 migrations, all with valid 14-digit timestamp prefixes.
- No duplicate versions.
- No empty or near-empty migrations flagged.
- Lexicographic order == timestamp order (deterministic replay).

## External prerequisites (platform, not repository omissions)

Baseline dump `20251209160634_remix_migration_from_pg_dump.sql`
creates the following extensions and depends on Supabase-managed
schemas that must be present on any replay target:

- Extensions: `pg_cron`, `pg_graphql`, `pg_stat_statements`,
  `pgcrypto`, `plpgsql`, `supabase_vault`, `uuid-ossp`, `vector`
  (pgvector).
- Schemas: `auth`, `storage`, `vault`, `graphql`, `extensions`.
- Roles: `anon`, `authenticated`, `service_role`, plus
  `supabase_admin`, `supabase_auth_admin`, `supabase_storage_admin`.

These are documented as external prerequisites in
`migration-replay-specification.md`, not as repository defects.

## B1 corrective migration

- File: `20260724005954_e17e6492-dc51-46f8-bb87-642071501b8b.sql`.
- SHA-256: `d943d072e1c50d23e2d1ae3af369c1742a84ce382ab6cdd7f099af139e6dfcea`.
- Position: **18 of 18 (last).**
- Static reconciliation: **26/26 checks pass.**
- Confirms in source that:
  - The three self-manage escalation policies
    (`user_roles_insert_own`, `_update_own`, `_delete_own`) are
    dropped, along with legacy `"Users can * own roles"` variants.
  - `INSERT`, `UPDATE`, `DELETE` on `public.user_roles` are revoked
    from `anon` and `authenticated`.
  - `SELECT` is granted to `authenticated`; `ALL` to `service_role`.
  - `role_change_audit` is created **before** the admin functions
    that write to it.
  - `admin_assign_role` and `admin_revoke_role` are `SECURITY DEFINER`
    with `SET search_path = public`, require `is_approved_user` and
    `has_role(_actor, 'security_admin')`, `REVOKE ALL ... FROM PUBLIC`,
    then `GRANT EXECUTE ... TO authenticated`.
  - Only `user_roles_read_own` remains as the SELECT policy on
    `user_roles`, scoped to `auth.uid() = user_id`.
- Every migration between the P0 introducer (file 10) and B1 (file
  18) was scanned for reintroduction of the escalation policies or
  DML grants on `user_roles` to `anon`/`authenticated`: **zero
  matches.**

## Validation checks run (static only)

| Check | Result |
|---|---|
| Migration inventory validator (`inventory-validator.json`) | PASS |
| Duplicate-version / malformed-timestamp / empty-file scan | 0 findings |
| Dependency reconciliation (`migration-dependency-graph.json`) | PASS |
| B1 policy/grant regression scan (`verify-b1-static-reconciliation.mjs`) | 26/26 PASS |
| Production-perimeter enforcer (`verify-production-perimeter.mjs`) | PASS — 155 functions inventoried, 0 allowlisted |
| `git diff --check` | PASS (no whitespace/conflict markers) |
| B7.4G evidence checksum verify (`sha256sum -c SHA256SUMS`) | PASS |

**Not run** (out of B7.5 scope): full Vitest suite, Playwright full
run, ESLint gate, deploy/undeploy checks, remote DB queries.

## What is not proven by this checkpoint

- Effective remote RLS and authorization state on Lovable Cloud.
- That the accidental remote migration observed earlier matches the
  repository chain.
- That every user's `user_roles` rows are free of residue accrued
  during the window when the P0 policies existed.
- That every one of the 155 inventoried edge functions is
  undeployed remotely.

These are retained external blockers for later checkpoints.

## Static passing criteria — all met

- Every repository migration inventoried: **18/18.**
- Migration order deterministic: **yes** (lex == timestamp).
- Checksums recorded: **yes** (per-migration SHA-256 in inventory).
- Dependencies and external prerequisites mapped: **yes.**
- B1 corrective positioned and reconciled: **yes, last, PASS.**
- Later migrations do not reintroduce the escalation path: **yes** (no later migrations exist).
- Unresolved replay risks classified: **yes** (see risk register and replay spec).
- Reproducible disposable replay specification exists: **yes** (`migration-replay-specification.md`).
- No migration or remote resource modified: **yes.**

## Handoff to B7.6

B7.5 hands the following to B7.6 as blockers still requiring
authorized work:

1. Credential rotation.
2. Effective remote RLS and authorization verification.
3. Remote function-undeployment verification (the 155 functions).
4. Reconciliation of the accidental remote migration against authoritative remote history.
5. Full migration-chain replay in an approved disposable Supabase-compatible environment per `migration-replay-specification.md`.
6. Verification that suitable validated pilot records exist remotely.

**Hard stop.** B7.6 and Checkpoint C are not started.