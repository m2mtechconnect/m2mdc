# Published verification

## Status: NOT PUBLISHED

Publication is gated on the canary passing. The canary did not pass
(`canary-results.md`), so no new build was published.

- Live build remains the protected baseline: `assets/index-BLh1dE2h.js`,
  source revision `ae7c8049`, manifest 1.1.0, host
  https://auradc.m2mtechconnect.com
- Default demonstration facility on the published host is unchanged.
- Capability totals are unchanged: AURA_NATIVE 7, DSX_ALIGNED 5,
  NVIDIA_INTEGRATED 0, SIMREADY_VALIDATED 0, PLANNED 2, BLOCKED 0,
  UNAVAILABLE 2. Importing NVIDIA reference data did not create an
  `NVIDIA_INTEGRATED` capability and did not promote anything to
  `SIMREADY_VALIDATED`.
