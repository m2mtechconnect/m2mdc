apiVersion: v1
kind: Service
metadata:
  name: {{ include "aura-web.fullname" . }}
  labels:
    {{- include "aura-web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
  selector:
    {{- include "aura-web.selectorLabels" . | nindent 4 }}
