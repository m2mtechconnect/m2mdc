Phase 1B.9 — Consolidation Gate Outputs
=======================================

Commit at time of run: 39ed8d95be847fc8b6af2403005fd86fed005b98
Reproduce with the commands in docs/remediation/phase-1b-baseline.md §3.

1. tsc -p tsconfig.app.json --noEmit
   Exit: 0. Errors: 0. Authoritative type gate — PASS.

2. npm run build
   Exit: 0. SEO gate: 0 errors / 0 warnings. Artifacts: index.html, robots.txt,
   sitemap.xml. Report: dist/seo-report.json. PASS.

3. Vitest full suite (npx vitest run --pool=forks --no-file-parallelism)
   Test Files: 148 failed | 64 passed (212)
   Tests:      236 failed | 1019 passed | 103 skipped (1358)
   Duration:   557.31s.
   Failure identity set: subset of baseline (236 == baseline 236) — PASS.

4. Vitest targeted (src/simulation)
   14 files / 112 tests — 112 passed. PASS.

5. Playwright truth-in-UI (npx playwright test --config=playwright.truth.config.ts)
   50 passed (3.7m). PASS.
   Note: runtime count increased from 47 → 50 as Phase 1B.2a added
   `simulation-facade.spec.ts` (3 tests). No prior test regressed.

6. Evidence integrity — Phase 1A.3 anchor (SHA256SUMS.txt in phase-1a3/):
   27/27 pngs REGENERATED by this run of `screenshots.spec.ts`.
   The anchor file itself is unchanged; the working-tree pngs now differ
   because Playwright always rewrites them. Fresh per-file digests for
   the regenerated set are recorded here as
   `docs/remediation/evidence/phase-1b9/screenshots-SHA256SUMS.txt`.
   All 27 files present, byte-stable within a single run.