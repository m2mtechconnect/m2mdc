# PR-0.1 Checkpoint B7.6 — Disposable Complete-Chain Replay

**Baseline anchor:** `d7c3da3eb7b8dfc69e444857d09ff5a3ab510679`
**Working tree:** clean at start; only `b7.6/` evidence written.
**Remote infrastructure contacted:** NO.
**Migrations executed:** NO.
**Disposable environments created:** 0.
**Verdict:** **BLOCKED — APPROVED SUPABASE-COMPATIBLE ENVIRONMENT UNAVAILABLE.**

## Why B7.6 stopped at preflight

Every fail-closed gate in §2 evaluated to FAIL. The full evaluation
is machine-recorded in `replay-preflight.json`; the summary:

| Gate | Result | Reason |
|---|---|---|
| DB host resolves only to loopback / isolated container network | FAIL | Only reachable DB host is the production Supabase pooler (`aws-1-eu-west-2.pooler.supabase.com`). No loopback Postgres provisioned; no container runtime available to isolate one. |
| Disposable DB + container names unique to B7.6 | FAIL | No container runtime (`docker`, `podman`, `colima`) is present in the sandbox. |
| No remote Supabase project reference configured | FAIL | `PGHOST/PGUSER/PGDATABASE` env vars point at the production project. Using them would violate the hard boundary. |
| Required schemas and roles supplied by approved local Supabase-compatible baseline | FAIL | Baseline dump references `auth.users`, `auth.uid()`, `auth.role()`, `vault.secrets`, `vault.decrypted_secrets`; the `supabase_*` roles are supplied by the Supabase managed stack. Rule §3 forbids hand-written substitutes. |
| Required extensions installable | FAIL | Local nix Postgres 17.9 lacks `pg_cron`, `pg_graphql`, `supabase_vault` (needs `pgsodium`), and `vector` (pgvector). Rule §2 forbids downloading unapproved tooling merely to pass. |
| Teardown removes DB and volumes without collateral | N/A | No isolatable environment could be created. |

Additionally the local `postgres` binary is version **17.9**; B7.5's
replay specification requires **15.x** to match Lovable Cloud
production. Even if managed prerequisites could be assembled, the
version-parity gate would still fail.

## What was preserved

- Git HEAD, migration count (18) and full per-file SHA-256 map
  recorded in `replay-preflight.json`.
- B7.5 evidence checksums re-verified (`SHA256SUMS` OK).
- B7.4G evidence checksums re-verified (`SHA256SUMS` OK).
- Production-perimeter enforcer re-run: **PASS** — 155 functions
  inventoried, 0 allowlisted (`perimeter.log`).
- Pilot-bundle canary re-run: **PASS** — 7 modules scanned, 0
  forbidden imports reachable (`pilot-bundle-canary.log`).
- B1 static reconciliation re-run: **26/26 PASS**
  (`b1-static-rerun.json`).
- `git diff --check`: **PASS** (`git-diff-check.log`, zero lines).
- Migration, application, edge-function, and CI source: unchanged.
- Production function allowlist: unchanged (0 entries).

## What was intentionally NOT run

Per §7, the full local gate suite is a completion criterion —
**not** a substitute when preflight fails. Running Vitest,
Playwright, or ESLint suites here would report a green state that
is unrelated to the replay blocker and could be misread as B7.6
progress. Their execution is deferred to the run that follows
preflight success. Prior anchors remain authoritative:

- Pilot adapter unit tests: 18/18 PASS (B7.4G).
- Pilot Playwright suite: 11/11 scenarios PASS with zero
  `/functions/v1/*` calls (B7.4G).
- B1 static reconciliation: 26/26 PASS (B7.5).

## Network and mutation guard

`b7.6-network-evidence.json`:

- Remote Supabase requests: 0.
- Production `/functions/v1/*` calls: 0.
- Lovable Cloud requests: 0.
- Storage / realtime mutations outside a disposable environment: 0.
- Credentials or tokens present in any B7.6 artifact: 0.

`b7.6-teardown-evidence.json`:

- Disposable databases / containers / volumes created: 0.
- Shared development infrastructure touched: NO.
- Repository migrations unchanged: YES.
- Application source, CI configuration, function allowlist:
  unchanged.

## Blockers

`b7.6-blocker-register.tsv` records six new P0/P1 platform blockers
(B7.6-BLK-01 through 06) and preserves the five external blockers
carried from B7.5 (B7.5-EXT-01 through 05).

## Passing criteria — none met (blocked)

| Criterion | Status |
|---|---|
| Replay A and Replay B complete successfully | UNMET — not executed |
| Both replays use the exact checksummed B7.5 order | UNMET — not executed |
| Final schema fingerprints match | UNMET — not produced |
| B1 runtime authorization tests pass | UNMET (static reconciliation still PASS) |
| Pilot read-model local RLS tests pass | UNMET (adapter unit tests still PASS) |
| All PR-0.1 security and pilot suites pass | Deferred — not executed |
| No new full-suite regression introduced | Deferred |
| Production build, perimeter enforcement, bundle canary pass | Perimeter PASS, canary PASS; production build deferred |
| No prohibited remote request or mutation | PASS (0) |
| Disposable environments removed | N/A (never created) |
| Migration, application, function, CI source unchanged | PASS |

## Recommended next authorization

Provide an **approved Supabase-compatible disposable environment**
(any one of):

1. A sandbox with `docker` or `podman` and permission to pull the
   official `supabase/postgres:15.x` image (or the `supabase start`
   toolchain), OR
2. An ephemeral, throw-away Supabase project with credentials that
   are provably distinct from production and staging, and whose
   destruction the operator will confirm after B7.6, OR
3. A managed local build of PostgreSQL 15 that ships `pg_cron`,
   `pg_graphql`, `supabase_vault` (+ `pgsodium`), and `pgvector`,
   plus a scripted stand-up of the `auth`, `storage`, `vault`,
   `realtime`, `graphql` schemas and `supabase_*` roles from
   Supabase's official bootstrap.

Once available, B7.6 can resume from `replay-preflight.json` with
**no source changes required** — the repository is already frozen
at the correct anchor.

## External blockers retained after B7.6

All five B7.5 external blockers remain open and are unchanged:

1. Credential rotation.
2. Effective remote RLS + approved-user authorization verification.
3. Remote function-undeployment verification (155 functions).
4. Reconciliation of the accidental remote migration against authoritative remote history.
5. Confirmation that suitable validated pilot records exist remotely.

**Hard stop.** Checkpoint C is not started. No remote infrastructure
will be contacted without a separate authorization.