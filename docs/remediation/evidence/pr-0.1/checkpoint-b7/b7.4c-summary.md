# PR-0.1 Checkpoint B7.4C - Approved-User Pilot Surface Decoupling

**Verdict:** B7.4C BLOCKED - APPROVED-USER WORKFLOW NOT OPERABLE
**Anchor:** git HEAD `15bc43860593053c739230df0f2a3b9891ab5b55`
**Mode:** no remote infrastructure was contacted; the production function allowlist remains empty; no edge function was hardened, deployed or restored; no database migration was changed.

## 1. B7.4B verdict correction

B7.4B is corrected to **B7.4B BLOCKED - SELECTED WORKFLOW NOT OPERABLE**. Five public routes alone do not constitute the authorized controlled pilot, which is an approved-user, read-only AURA experience. The public-route findings (that `/`, `/login`, `/forgot-password`, `/sign-out` render truthfully with zero edge-function invocation) are preserved.

Public-pilot re-baseline (post-correction):

| Route | Identity requirement | Writes? | Notes |
|-------|----------------------|---------|-------|
| `/`               | anonymous | no  | landing |
| `/login`          | anonymous | writes only via `supabase.auth.signInWithPassword` (Auth SDK, not an edge function; establishes a session) | included |
| `/forgot-password`| anonymous | writes only via `supabase.auth.resetPasswordForEmail` (Auth SDK); does NOT complete recovery | included as recovery-entry only; does NOT count toward pilot workflows |
| `/sign-out`       | anonymous or approved | writes only via `supabase.auth.signOut` (Auth SDK) | included |
| `/onboarding`     | anonymous | **YES - `supabase.from('onboarding_submissions').insert(...)`** at `src/pages/Onboarding.tsx:108` | **RECLASSIFIED** to write-deferred; moved from `production_routes` to `production_blocked_routes` |

Git HEAD: `15bc43860593053c739230df0f2a3b9891ab5b55`. Working tree carries only the B7.4C edits enumerated below.

### Checksums of B7.4B evidence at the moment B7.4C started

```
cfd41bd016866279fe0677553b9359cc0d5bc91d8f8b0de0bf96fbb57795b530  negative-tests-consolidated.json  (rewritten in B7.4C)
c5df733241d3e5b8c66075208a47295e4c34f6ce0aa54323f339ce572ed0fe96  pilot-function-candidates.json    (rewritten in B7.4C)
b137ab70a1f9b79738cb85c5fdd38fa73e9b736097d9cb840c14d2fab4b37ebc  pilot-scope-decision.json         (rewritten in B7.4C)
16208db97350d3140a440ee11845419744783380f87d977247fe084e1fdef8db  function-callsite-reconciliation.tsv  (retained from B7.4B)
a2cffe734efae38e5e58c17a2e06df31ee650413e2d38aab0851d253b4616be7  pilot-route-matrix.tsv            (rewritten in B7.4C)
b20b2446e67f42030f885b5283ae7e63903cb12ae1d58d3c47816bbfd2e015a7  route-classification.tsv          (patched: /onboarding -> production-blocked)
8f276ac91b8ff2fea2d0a271bba9b39099cbe5cf6830dc9effc9ea2003693345  b7.4b-summary.md                  (correction banner prepended)
33cd1dd56ccb645f96932e421116d0864618c14afc4e17ec11eed022583a56fc  password-recovery-reconciliation.md (rewritten in B7.4C)
```

`route-classification.tsv` and `negative-tests-consolidated.json` both exist and remain current. The perimeter enforcer PASSES on the real repo tree at HEAD (`155 functions inventoried, 0 allowlisted`), and all 10 negative fixtures (NEG-A through NEG-I plus CLEAN) behave as expected.

## 2. Approved-user pilot surface - selection attempt

Required capabilities: authentication; approved-user enforcement; primary AURA overview; one representative read-only asset or metric inspection; provenance / validation / availability status; logout.

Smallest existing candidate route set considered: `/` (Dashboard), `/analytics`, `/operations`, `/intelligence`, `/account/profile`, `/help`, `/sign-out`. Full row-by-row assessment: `approved-user-pilot-routes.tsv`.

**No existing route satisfies the criteria without architectural expansion.** Details:

- **Every candidate authed route mounts inside `<Layout>`** (`src/App.tsx:202-275`). `Layout` unconditionally imports and initializes excluded capabilities: `HealthBadges` (invokes `health`), `useTokenRefresh` (invokes `zapier-auto-refresh` and `zapier-refresh-token` in the background), `GlobalSearchBar` (invokes `search-suggestions` on interaction), `CoPilotPanel`, `CoPilotBubble` (invoke `copilot-search`, `copilot-chat-simple`), `AOCQuickAccessButton`, `DataCentreSelector`. These are on the exclusion list.
- **The nearest AURA overview** (`src/pages/Dashboard.tsx:172`) invokes `ai-systems-unified` on initial render and offers `systems-delete` (`Dashboard.tsx:236`) as a permitted action. The fallback path on non-2xx renders fabricated rows and hardcoded metrics.
- **The nearest telemetry surfaces** (`/analytics`, `/operations`, `/intelligence`, all mapped to `IntelligenceDashboard`) invoke `ops-overview` on initial render (`src/pages/IntelligenceDashboard.tsx:192`).
- **The remaining candidates** (`/account/profile`, `/help`) are neither an AURA overview nor a read-only asset/metric inspection; they still mount inside `<Layout>` and therefore still initialize excluded capabilities.

## 3. Decoupling attempted, decoupling declined

The user permits "a minimal pilot-safe layout or route shell". A pilot-safe `<Layout>` alternative that omits `HealthBadges`, `CoPilotPanel`, `CoPilotBubble`, `GlobalSearchBar` and `useTokenRefresh` is technically feasible, but on its own it does not create an operable pilot: the pages routed through it (Dashboard, IntelligenceDashboard) still invoke blocked functions on initial render and still fall back to fabricated data.

The user's guidance explicitly forbids adding product capabilities. Building a new AURA overview page and a new read-only asset/metric inspection page from direct table reads (the only remaining path to a zero-edge-function pilot) is an **architectural expansion beyond decoupling**. This checkpoint therefore stops per the authorized escape hatch:

> "If the overview and inspection workflow cannot be separated from excluded capabilities without architectural expansion, stop with: B7.4C BLOCKED - APPROVED-USER WORKFLOW NOT OPERABLE."

No pilot-safe layout was introduced in this checkpoint because introducing it without operable overview / inspection pages would be evidence of a change that does not close the gap.

## 4. Backend dependency reconciliation

Row-per-dependency: `pilot-data-dependency-matrix.tsv`. Summary:

| Route | Blocked-function initial-render invocations | Blocked-function permitted-action invocations | Layout-mounted background/interaction invocations | Read-only? | Pilot disposition |
|-------|--------|--------|--------|--------|-------|
| `/`, `/dashboard`          | `ai-systems-unified` | `systems-delete` | `health`, `zapier-auto-refresh`, `zapier-refresh-token`, `search-suggestions`, `copilot-search`, `copilot-chat-simple` | no (delete action) | BLOCKED |
| `/analytics`, `/operations`, `/intelligence` | `ops-overview` | (view-only) | same Layout set as above | yes | BLOCKED |
| `/account/profile`         | none (direct `profiles` read) | (writes on save) | same Layout set | partially | NOT A PILOT SURFACE |
| `/help`                    | none | none | same Layout set | yes | NOT A PILOT SURFACE |
| `/sign-out`                | none | `supabase.auth.signOut` (Auth SDK) | none | writes session termination | OPERABLE (used by both shells) |

Direct table reads (e.g. `public.profiles` via `/account/profile`) rely on RLS. Effective remote RLS cannot be verified from the sandbox and is retained as an **external blocker**; a zero-edge-function claim that leans on direct reads cannot be marked passed at B7.4C.

## 5. Workflow exercise

No approved-user workflow was exercised because none is operable. The exercise plan (anonymous denial, unapproved denial, approved-user overview render, one asset/metric inspection, provenance visible, unavailable-not-fabricated, zero blocked functions, zero writes, excluded routes not directly loadable, hidden components with no background requests, logout return, keyboard/screen-reader coverage) is authoritatively **not-run** at B7.4C. Retry belongs in B7.5 after decoupling and operable read-only pages exist.

Public shell was re-exercised in read-only form (unauthenticated navigation to `/`, `/login`, `/forgot-password`, `/sign-out`); no new evidence beyond B7.4B was collected because those findings remain valid.

## 6. Evidence delivered

| File | Status |
|---|---|
| `negative-tests-consolidated.json` | UPDATED (schema_version 3, checkpoint B7.4C, 10/10 PASS at HEAD) |
| `route-classification.tsv`         | UPDATED (`/onboarding` moved from public-pilot to production-blocked) |
| `pilot-route-matrix.tsv`           | UPDATED (adds `counts_toward_pilot_workflows`, marks approved-user pilot BLOCKED, reclassifies `/onboarding`) |
| `function-callsite-reconciliation.tsv` | UNCHANGED from B7.4B (63 functions across 92 sites remains accurate) |
| `pilot-function-candidates.json`   | UPDATED (`pilot_required_candidate_count = 0`; informational candidates recorded for B7.5) |
| `pilot-scope-decision.json`        | UPDATED (schema_version 2, `/onboarding` write-deferred, approved-user pilot BLOCKED with reasons) |
| `password-recovery-reconciliation.md` | UPDATED (recovery split into entry / completion / overall; overall NOT operable, does NOT count) |
| `approved-user-pilot-routes.tsv`   | NEW (per-route decoupling assessment) |
| `pilot-data-dependency-matrix.tsv` | NEW (per user-action dependency matrix with external blockers) |
| `b7.4b-summary.md`                 | UPDATED with a superseding banner; public-route findings preserved |
| `b7.4c-summary.md`                 | NEW (this document) |

## 7. External blockers retained

- Credential rotation (VITE_LOVABLE_API_KEY, Omniverse host) - out-of-band.
- Effective remote authorization verification (Supabase RLS on `profiles`, `user_roles`, `onboarding_submissions`) - requires an approved Supabase-compatible environment.
- Remote function undeployment verification for the 155 inventoried functions - not verifiable from the sandbox.
- Accidental migration change-management reconciliation - unchanged.
- Full migration-chain replay in an approved Supabase-compatible environment - unchanged.

None of these are considered passed by B7.4C.

## 8. What was NOT done (hard-boundary respected)

- No remote infrastructure was contacted or mutated.
- The production function allowlist was not modified (`production_functions: []`).
- No edge function was hardened, deployed, or restored.
- No database migration was changed.
- No product capability was added (no new overview page, no new inspection page).
- No AI, Copilot, Omniverse, integration, or simulation capability was restored.
- No fabricated or synthetic production metric was introduced.
- B7.5, B7.6 and Checkpoint C were not started.

## 9. Next authorized step (proposal only)

Two orthogonal follow-ups, in either order, would unblock B7.5:

1. **Layout decoupling + minimum read-only overview and inspection pages.** Extract the excluded-capability mounts behind a `pilotLayout` flag; ship a truthful read-only overview backed by a direct RLS-guarded table read (with fail-closed and typed-unavailable states), and a single read-only asset/metric inspection page. This is architectural expansion and must be explicitly authorized.
2. **Or, disposition of the two informational candidates** (`ai-systems-unified`, `ops-overview`) through the full 14-column function assessment plus positive and denial tests, followed by an allowlist entry - after Layout decoupling. This is what "B7.4C READY FOR FUNCTION DISPOSITION" would require, and it is not authorized in this checkpoint.

## Completion response

**B7.4C BLOCKED - APPROVED-USER WORKFLOW NOT OPERABLE**
